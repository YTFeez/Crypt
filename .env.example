# Optionnel — sans ces variables, Talkeo fonctionne en mode local (données dans le navigateur).
# Sur le VPS / production, renseignez vos clés Supabase :

VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Optionnel — webhook pour envoyer le code à 6 chiffres (Edge Function, Resend, etc.)
# E-mails IONOS depuis support@talkeo.fr (voir .env.production.example)
# VITE_API_URL=http://127.0.0.1:8787
# VITE_VERIFICATION_EMAIL_URL=https://votredomaine.fr/api/send-verification
